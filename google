import webbrowser


def google(web, path='open -a /Applications/Google\ Chrome.app  %s'):
    if not isinstance(web, str):
        raise ValueError('Принимается только str')
    else:
        webbrowser.get(path).open(web)


google(input())
